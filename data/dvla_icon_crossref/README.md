Generated DVLA icon coverage outputs.

This folder is intentionally gitignored to avoid repository bloat.

Regenerate locally with:

```powershell
python scripts/crossref_dvla_vehicle_icons.py
python scripts/build_dvla_vehicle_icon_lookup.py
```

Runtime only needs:

- `data/dvla_vehicle_icon_lookup.json`

